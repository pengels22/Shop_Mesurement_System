# Shop_Measurement_System

## Structure
- `Core/` contains the Flask backend, services, routes, models, config, and project data.
- `Web/Templates/` contains UI page templates.
- `Web/Static/` contains CSS and JavaScript.

## Run
```bash
pip install -r requirements.txt
python Core/run.py
```
